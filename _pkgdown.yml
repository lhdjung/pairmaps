url: ~

lang: en

home:
  title: New column-pair mapping functions
  description: >
         The pairmaps package provides `as_colpair_mapper()`
         which modifies a function so that it is applied to each
         pair of columns in a data frame. It is a wrapper around
         `corrr::colpair_map()`.

template:
  bootstrap: 5
  bootswatch: flatly
  bslib:
    link-color: "#ca225e"
    code-color: "#ca225e"
    navbar-dark-hover-color: "#ca225e"
    navbar-dark-active-color: "#f9ea6b"


reference:

- title: Function factory
- contents:
  - as_colpair_mapper

- title: Example factory-made functions
- contents:
  - vary
  - covary

- title: Helpers
- contents:
  - get_mapped_function
  - get_mapped_function_name
  - is_pairmaps_df

